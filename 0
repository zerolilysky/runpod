from iv_cnn_runner import run
run(pattern="om_panel_top100*.pickle", window="roll6", refit_months=24,
    denoise="lowrank20")            # in-place denoise (best for the CNN)
run( drop_train_noise_q=0.90, drop_metric="roughness")   # drop noisy train rows
